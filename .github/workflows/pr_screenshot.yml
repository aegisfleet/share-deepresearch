name: PR Screenshot

on:
  pull_request:
    types: [opened, synchronize]

jobs:
  capture_screenshot:
    runs-on: ubuntu-latest
    permissions:
      pull-requests: write
      actions: read # Needed to construct artifact URL if we get sophisticated, for now GITHUB_SERVER_URL and GITHUB_REPOSITORY should be enough
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Node.js dependencies (Puppeteer, http-server)
        run: npm install puppeteer http-server

      - name: Setup Ruby and Jekyll
        uses: ruby/setup-ruby@v1
        with:
          ruby-version: '3.1' # Specify a Ruby version compatible with the project's Gemfile
          bundler-cache: true # Automatically runs bundle install and caches gems

      - name: Build Jekyll site
        run: bundle exec jekyll build
        env:
          JEKYLL_ENV: production

      - name: Start HTTP server
        run: npx http-server _site --port 8080 &

      - name: Wait for server to start
        run: sleep 5

      - name: Capture screenshot with Puppeteer
        id: capture # Keep id in case it's useful later
        run: |
          node -e "
          const puppeteer = require('puppeteer');
          (async () => {
            const browser = await puppeteer.launch({ headless: 'new', args: ['--no-sandbox', '--disable-setuid-sandbox'] });
            const page = await browser.newPage();
            await page.setViewport({ width: 1280, height: 800 });
            try {
              await page.goto('http://localhost:8080', { waitUntil: 'networkidle0' });
              await page.screenshot({ path: 'screenshot.png', fullPage: true });
              console.log('Screenshot captured: screenshot.png');
            } catch (err) {
              console.error('Error capturing screenshot:', err);
              process.exit(1);
            } finally {
              await browser.close();
            }
          })();
          "

      - name: Upload screenshot artifact
        uses: actions/upload-artifact@v4
        with:
          name: screenshot # Artifact name
          path: screenshot.png
          retention-days: 7

      - name: Comment on PR with Screenshot Artifact Link
        uses: peter-evans/create-or-update-comment@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          issue-number: ${{ github.event.pull_request.number }}
          body: |
            A screenshot of the homepage has been captured for this PR.

            You can download and view the screenshot from the [workflow run artifacts for this commit](${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}).
            Look for an artifact named "screenshot".

            ---
            *Screenshot captured by GitHub Actions.*
          edit-mode: replace
